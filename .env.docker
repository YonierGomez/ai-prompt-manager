# Configuración para Docker
NODE_ENV=production
DATABASE_URL=file:./data/dev.db
NEXT_TELEMETRY_DISABLED=1

# Configuración de volúmenes
# Los datos se persistirán en volúmenes Docker nombrados:
# - ai_prompt_data: Base de datos y configuraciones
# - ai_prompt_uploads: Archivos subidos por usuarios (futuro)

# Para hacer backup de los datos:
# docker run --rm -v ai_prompt_data:/data -v $(pwd):/backup alpine tar czf /backup/ai-prompt-backup.tar.gz -C /data .

# Para restaurar backup:
# docker run --rm -v ai_prompt_data:/data -v $(pwd):/backup alpine tar xzf /backup/ai-prompt-backup.tar.gz -C /data
